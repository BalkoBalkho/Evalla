/**
 * 
 * @param {string} code 
 */

module.exports = (code) => {
    
    let backTickIndexArray =[]
    let code_state = 0
    let prev_chr

    for (var i = 0; i < str.length; i++) {


        if (code[i] == '`') {
          if (code_state == 0) {

          } else {
              if (code_state)
          }
        }

        prev_chr = code[i]
      }


}